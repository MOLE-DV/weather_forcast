import react from "react";
import Search from "./Search";

function Header(){
    return (
        <header id="header">
            <Search placeholder="Search"/>
        </header>
    );
}

export default Header;